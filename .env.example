# Public client configuration only. Copy to .env.local and restart Vite.
# Project URL and publishable/anon key are available in Supabase project settings.
# Never put a service-role key, database password, or Google client secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
